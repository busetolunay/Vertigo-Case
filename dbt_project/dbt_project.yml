

name: 'dbt_project'
version: '1.0.0'

# which "profile" dbt uses for this project.
profile: 'dbt_project'

# where dbt should look for different types of files.
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:         # directories to be removed by dbt clean
  - "target"
  - "dbt_packages"

models:
  dbt_project:
